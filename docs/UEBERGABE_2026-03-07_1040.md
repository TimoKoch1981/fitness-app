# Uebergabe 2026-03-07 10:40 — Phase 2: Early Triggers + PED-Sync + Mesozyklus-Review

## Abgeschlossene Arbeit

### Phase 1 (bereits deployed — Commit 6867134)
- Post-Session-Analyse, Double Progression, RIR-Feedback
- Deployed auf fudda.de ✅

### Phase 2.1: Early Triggers (Block C)
**Geaendert:** `src/lib/ai/deviations.ts`
- 6 neue Training-spezifische Trigger-Checks:
  1. **Plateau** (plateau_exercises.length > 0) → priority 3, info
  2. **Low Completion** (<70%) → priority 2, warning
  3. **Joint Pain** (rating >= threshold) → priority 2, warning
  4. **RPE Drift** (rpe_drift_exercises.length > 0) → priority 3, info
  5. **Sleep Deficit** (5+ Tage <6h) → priority 2, warning
  6. **Missed Sessions** (>30% geplant verpasst) → priority 3, info
- 6 neue Suggestion-Chips in `getDeviationSuggestions()`
- Nutzt `SessionFeedback.auto_calculated` aus Recent Workouts
- Nutzt `ReviewConfig.review_triggers` Schwellwerte

### Phase 2.2: PED-Phasen-Sync (Block C)
**Neue Datei:** `src/features/workouts/hooks/usePEDPhaseSync.ts`
- useEffect: monitored profile.cycle_status Aenderungen
- Bei Phasenwechsel (blast→cruise→pct→off): `getSmartPreset()` aufrufen
- Review_config automatisch aktualisieren (mesocycle_weeks, deload_week)
- Mesozyklus reset (current_week=1, mesocycle_start=today)
- Nur fuer Power+ mode, ai_supervised Plans

### Phase 2.3: Mesozyklus-Review (Block C)
**Neue Datei:** `src/features/workouts/hooks/useMesocycleCheck.ts`
- Pure useMemo Hook: berechnet currentWeek aus mesocycle_start
- Returns: `{ reviewDue, currentWeek, totalWeeks, daysUntilReview, hasConfig }`

**Neue Datei:** `src/features/workouts/utils/mesocycleReview.ts`
- `generateReviewSummary(workouts, planDays, reviewConfig)` → `MesocycleReviewSummary`
- 4 Empfehlungen: continue / deload / swap / overhaul
- Analysiert: avgCompletionRate, plateauExercises, sessionsWithPain, avgFeeling

**Geaendert:** `src/features/workouts/hooks/useSaveWorkoutSession.ts`
- Step 4: `updateMesocycleWeek()` — aktualisiert current_week nach Session-Save

**Geaendert:** `src/features/workouts/components/TrainingPlanView.tsx`
- `usePEDPhaseSync()` eingebunden
- `useMesocycleCheck()` eingebunden
- Mesozyklus-Review Banner: indigo Info-Karte wenn reviewDue=true
- "Review starten" Button → oeffnet Buddy-Chat mit Auto-Message

## Build-Status
- `npm run build` → 0 TS-Fehler ✅
- 94 PWA Precache Entries
- fudda.de referenziert ✅, kein localhost ✅

## Dateien (Zusammenfassung)

| Aktion | Datei |
|--------|-------|
| GEAENDERT | `src/lib/ai/deviations.ts` (+6 Trigger, +6 Suggestion Chips) |
| NEU | `src/features/workouts/hooks/usePEDPhaseSync.ts` |
| NEU | `src/features/workouts/hooks/useMesocycleCheck.ts` |
| NEU | `src/features/workouts/utils/mesocycleReview.ts` |
| GEAENDERT | `src/features/workouts/hooks/useSaveWorkoutSession.ts` (+updateMesocycleWeek) |
| GEAENDERT | `src/features/workouts/components/TrainingPlanView.tsx` (+PED-Sync, +Review-Banner) |

## Naechste Schritte (Phase 3 — noch ausstehend)
- Phase 3.1: Buddy-Nachfrage (useAISupervisedOffer.ts)
- Phase 3.2: Review-Dialog (ReviewDialog.tsx + reviewChanges.ts + useApplyReviewChanges.ts)
- Dann: Uebergabedatei, Commit, Push, Deploy, TODO.md + DEPENDENCIES.md

## Git-Status
- Commit + Push: AUSSTEHEND (wird nach Pause gemacht)
- Deploy: AUSSTEHEND

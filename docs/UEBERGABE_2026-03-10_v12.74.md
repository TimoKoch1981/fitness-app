# Uebergabe 2026-03-10 — Plan bearbeiten + Plan bewerten

> **Session-Stand:** v12.74, deployed auf fudda.de, verifiziert
> **Git:** Commit `3c5a534` auf develop, gepusht
> **Version:** v12.74

---

## Was wurde in dieser Session gemacht

### 1. ExerciseListBar Pfeile-Fix (v12.73 → fudda.de)
- Pfeile groesser (h-5 w-5) und horizontal nebeneinander (statt vertikal gestapelt)
- Full-Row-DnD: `onPointerDown stopPropagation` vom Jump-Button entfernt — DnD funktioniert jetzt ueberall auf der Zeile
- GripVertical als separater visueller Hinweis

### 2. Plan bearbeiten + Plan bewerten (v12.74)

**Problem:** Buddy-Chips "Plan anpassen" und "Plan bewerten" oeffneten nur den KI-Buddy-Chat ohne Kontext — nicht nuetzlich.

**Loesung:** 4 Aenderungen:

| # | Datei | Aenderung |
|---|-------|-----------|
| 1 | `src/features/workouts/hooks/useTrainingPlans.ts` | Neuer `useUpdateTrainingPlan` Hook (Name, Split, Tage, Notizen) |
| 2 | `src/features/workouts/components/EditPlanMetaDialog.tsx` | **Neue Datei** — Bottom-Sheet Dialog, Felder vorausgefuellt, "Buddy helfen lassen" |
| 3 | `src/features/workouts/components/TrainingPlanList.tsx` | "Plan bearbeiten" + "Plan bewerten" Buttons im aufgeklappten Plan-Karten |
| 4 | `src/features/workouts/components/WorkoutsTabContent.tsx` | Buddy-Chips `plan_edit`/`plan_evaluate` abgefangen |

**Zusaetzlich:** `SPLIT_OPTIONS` in `CreatePlanDialog.tsx` als `export const` (fuer shared usage).

### 3. Verifikation auf fudda.de

| Check | Ergebnis |
|-------|----------|
| Plan aufklappen → Buttons sichtbar | OK |
| "Plan bearbeiten" → Dialog mit vorausgefuellten Feldern | OK |
| "Plan bewerten" → Buddy bewertet mit vollem Kontext (Tags + Uebungen) | OK |
| Buddy-Chip "Plan anpassen" → EditPlanMetaDialog | OK |
| Buddy-Chip "Plan bewerten" → Buddy mit Review-Kontext | OK |
| 0 TS-Fehler, Build OK (111 PWA entries) | OK |

---

## Geaenderte Dateien

| Datei | Aenderung | Zeilen |
|-------|-----------|--------|
| `useTrainingPlans.ts` | + `useUpdateTrainingPlan`, `UpdateTrainingPlanInput` | +44 |
| `EditPlanMetaDialog.tsx` | **Neue Datei** — Bottom-Sheet, teal Design, Buddy-Integration | +195 |
| `TrainingPlanList.tsx` | + Buttons, `handleReviewPlan`, `editingPlanMeta` State, `useInlineBuddyChat` | +67 |
| `WorkoutsTabContent.tsx` | + `plan_edit`/`plan_evaluate` Interception, `usePlanById`, `EditPlanMetaDialog` | +58 |
| `CreatePlanDialog.tsx` | `const` → `export const` fuer SPLIT_OPTIONS | +1/-1 |

---

## Offene Punkte / Naechste Schritte

1. **EditPlanMetaDialog: Tage-Anzahl-Aenderung** — Wenn User Tage von 3 auf 5 aendert, werden keine neuen TrainingPlanDays angelegt (nur Metadaten-Update). Fuer vollstaendige Tage-Verwaltung muesste man Days hinzufuegen/entfernen.
2. **Buddy-Chips optional ausblenden** — Die Chips "Plan anpassen"/"Plan bewerten" sind jetzt intercepted, koennten aber optional ausgeblendet werden wenn ein Plan aufgeklappt ist (da die Buttons im Karten sie ersetzen).
3. **Save-Feedback** — EditPlanMetaDialog schliesst nach Save, zeigt aber keinen Toast/Snackbar. Koennte man mit einem globalen Toast-System ergaenzen.

---

## Abhaengigkeiten

- `EditPlanMetaDialog` haengt ab von: `useUpdateTrainingPlan`, `useInlineBuddyChat`, `SPLIT_OPTIONS` aus CreatePlanDialog
- `TrainingPlanList` nutzt jetzt: `useInlineBuddyChat`, `EditPlanMetaDialog`, `handleReviewPlan`
- `WorkoutsTabContent` nutzt jetzt: `usePlanById` (fuer expanded plan data), `EditPlanMetaDialog`

---

## Git-Commits dieser Session

```
3c5a534 docs: Update FORTSCHRITT.md + TODO.md for v12.74
737247e feat: Plan bearbeiten + Plan bewerten — In-Card Buttons + Buddy-Chip Interception
b66ff4e fix: ExerciseListBar — Pfeile groesser+nebeneinander, Full-Row-DnD fix
62adf51 docs: Update FORTSCHRITT.md + TODO.md for v12.73
7ec6f17 feat: 4 UX-Verbesserungen — ExerciseListBar, Post-Creation-Flow, Buddy-Hilfe, Akkordeon
```

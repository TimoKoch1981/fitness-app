# ╔══════════════════════════════════════════════════════════════════╗
# ║  FitBuddy — Production Environment (Hetzner Self-Hosted)       ║
# ║  Copy to .env and fill in ALL values before docker compose up  ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── Domain ──────────────────────────────────────────────────────
DOMAIN=fitbuddy.app
SITE_URL=https://fitbuddy.app
API_EXTERNAL_URL=https://fitbuddy.app
ADDITIONAL_REDIRECT_URLS=https://fitbuddy.app

# ── Postgres ────────────────────────────────────────────────────
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=postgres

# ── JWT (generate with: openssl rand -base64 32) ───────────────
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL
JWT_EXPIRY=3600
ANON_KEY=CHANGE_ME_GENERATE_WITH_SUPABASE_CLI
SERVICE_ROLE_KEY=CHANGE_ME_GENERATE_WITH_SUPABASE_CLI

# ── PostgREST ───────────────────────────────────────────────────
PGRST_DB_SCHEMAS=public,graphql_public

# ── SMTP (Resend) ──────────────────────────────────────────────
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=re_CHANGE_ME_RESEND_API_KEY
SMTP_ADMIN_EMAIL=noreply@fitbuddy.app
SMTP_SENDER_NAME=FitBuddy

# ── Mailer URL Paths ───────────────────────────────────────────
MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify

# ── AI (OpenAI) — Server-seitig, NIE im Frontend! ──────────────
OPENAI_API_KEY=sk-CHANGE_ME_OPENAI_KEY

# ── Analytics ───────────────────────────────────────────────────
LOGFLARE_API_KEY=CHANGE_ME_RANDOM_STRING

# ── Realtime ────────────────────────────────────────────────────
SECRET_KEY_BASE=CHANGE_ME_GENERATE_WITH_OPENSSL_64_BYTES

# ╔══════════════════════════════════════════════════════════════════╗
# ║  Key Generation Commands:                                       ║
# ║                                                                ║
# ║  JWT_SECRET:      openssl rand -base64 32                      ║
# ║  SECRET_KEY_BASE: openssl rand -base64 64                      ║
# ║  POSTGRES_PW:     openssl rand -base64 24                      ║
# ║  LOGFLARE_KEY:    openssl rand -hex 16                         ║
# ║                                                                ║
# ║  ANON_KEY + SERVICE_ROLE_KEY:                                  ║
# ║    npx supabase-key generate --jwt-secret=YOUR_JWT_SECRET      ║
# ║    Or use: https://supabase.com/docs/guides/self-hosting       ║
# ╚══════════════════════════════════════════════════════════════════╝

# ╔══════════════════════════════════════════════════════════════════╗
# ║  FitBuddy — Environment Variables                              ║
# ║  Copy this file to .env.local and fill in your values           ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── Supabase ──────────────────────────────────────────────────────
# Local (Supabase CLI + Docker):
#   VITE_SUPABASE_URL=http://127.0.0.1:54321
#   VITE_SUPABASE_ANON_KEY=<from: npx supabase start>
#
# Cloud (supabase.com):
#   VITE_SUPABASE_URL=https://<project-ref>.supabase.co
#   VITE_SUPABASE_ANON_KEY=<from: Supabase Dashboard → Settings → API>
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── AI Provider ───────────────────────────────────────────────────
# Options: 'openai' | 'ollama' | 'supabase' (auto-detected if empty)
#
# Auto-detection:
#   - Cloud (https:// URL): defaults to 'supabase' (Edge Function proxy)
#   - Local (http:// URL):  defaults to 'openai' (direct API calls)
#
# VITE_AI_PROVIDER=openai

# ── OpenAI (for local development with direct API calls) ─────────
# Only needed when VITE_AI_PROVIDER=openai (local dev)
# In production, set OPENAI_API_KEY in Supabase Secrets instead!
# VITE_OPENAI_API_KEY=sk-...
VITE_OPENAI_MODEL=gpt-4o-mini

# ── Ollama (optional, local LLM) ─────────────────────────────────
# VITE_AI_PROVIDER=ollama
# VITE_OLLAMA_URL=http://localhost:11434
# VITE_OLLAMA_MODEL=llama3.1:8b

# ╔══════════════════════════════════════════════════════════════════╗
# ║  Server-Side Secrets (NOT in this file!)                        ║
# ║  Set these in Supabase Dashboard → Settings → Edge Function     ║
# ║  Secrets (for Cloud deployment):                                ║
# ║                                                                ║
# ║  OPENAI_API_KEY=sk-...        (used by ai-proxy Edge Function) ║
# ║  RESEND_API_KEY=re_...        (used for transactional emails)  ║
# ╚══════════════════════════════════════════════════════════════════╝
